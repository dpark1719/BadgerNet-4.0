# `data/raw/` (local only)

Put downloaded CSVs, PDFs, or exports here for ETL scripts. This directory is **gitignored** — do not commit licensed or scraped raw dumps.

Processed chart bundles belong in `frontend/public/data/` (generated by `backend/scripts/`).
